[package]
name = "gpui_ffi"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Rust FFI bindings for gpui"
publish = false

[lib]
crate-type = ["staticlib", "cdylib"]

[dependencies]
gpui = { workspace = true }
gpui_platform = { workspace = true }
